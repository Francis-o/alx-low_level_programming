#include "main.h"

/**
 * more_numbers - prints 0 - 14 ten times
 */

void more_numbers(void)
{
	int count;
	int num;
	while (count < 9)
	{
		count++
			for (num = 0; num <= 14; num++)
			{
				_putchar(num);
			}
		_putchar('\n');
	}
}
