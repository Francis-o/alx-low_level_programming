#include "main.h"

/**
 * _abs - Task 6
 * Description - Returns absolute value of number
 * @n: number
 * Return: integer
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
